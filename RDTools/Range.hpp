#pragma once

#ifndef TOOLS_RANDOM_RANGE
#define TOOLS_RANDOM_RANGE


/*
#include "Types.hpp"
#include <generator>
#include <range/v3/experimental/utility/generator.hpp>

// A simple coroutine returning a C++23 std::generator
template <typename T = int>
std::generator<T> range(T start, T end) {
    for (T i = start; i < end; ++i) {
        co_yield i; // Yields value and suspends execution
    }
}
*/

#endif